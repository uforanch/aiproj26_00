import sympy as sp
from sympy import diff
"""
import sympy as sp

# Define the variable
x = sp.Symbol('x')

# Define the sigmoid function manually
def sigmoid(z):
    return 1 / (1 + sp.exp(-z))

# Apply sigmoid to sigmoid
nested_sigmoid = sigmoid(sigmoid(x))

# Display the symbolic expression
sp.pprint(nested_sigmoid)
"""

